server:
  port: 8762
spring:
  application:
    name: provider-2
  cloud:
    nacos:
      config:
        enabled: false
        server-addr: 192.168.1.118:8848
        file-extension: yml
        prefix: provider-2
        group: loadbance-test
  profiles:
    active: dev
