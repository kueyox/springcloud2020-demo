spring:
  application:
    name: provider
  cloud:
    nacos:
      config:
        enabled: false
        server-addr: 192.168.1.118:8848
        file-extension: yml
        prefix: provider-1
        group: loadbance-test
  profiles:
    active: dev
