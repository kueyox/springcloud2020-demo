server:
  port: 8763
spring:
  application:
    name: consumer
  cloud:
    nacos:
      config:
        enabled: false
        server-addr: 192.168.1.118:8848
        file-extension: yml
        prefix: consumer
        group: loadbance-test
  profiles:
    active: dev


